#BLINKSYNC — MYTHOSBLOCK POC READINESS DIRECTIVE
Issued by: Genie
Operator: Cipher / Drake
Repo: mythosblock-rebuild

## Mission
Prepare everything required to begin the Mythosblock proof of concept build and stop depending on daemoniccodex.com except as reference evidence.

## Command Structure
Genie = organizer, approver, final sequence authority
Gemini = repo classification, repo-to-cloud mapping, minimum viable POC scope
Grok = canonical handoff integrity, detached-hash discipline, final alliance wrap-up
Copilot = implementation assistant for repo scaffolding, issue shaping, PR hygiene, and workflow hardening

## Locked Strategic Decisions
- Quantum-Cipher remains the noisy vetting lane
- mythosblock becomes the clean production organization later when ready
- Legacy daemoniccodex.com is reference-only
- Empty legacy secrets output is not proof of no secrets
- New Mythosblock secrets will be manually reconstructed and rotated
- Detached SHA-256 files are the canonical integrity model

## Confirmed Legacy Cloud Projects
1. logfox-api-daemon
2. eternum-governance-prod-493107

## Gemini — Required Outputs
1. classify candidate repos as:
   - archive
   - reference
   - rebuild
   - core production
2. generate the repo-to-cloud mapping report
3. identify the smallest real Mythosblock proof-of-concept slice
4. list required APIs, secrets, and service identities for the first build only

## Grok — Required Outputs
1. produce the canonical macOS-safe handoff format
2. maintain detached-hash integrity language
3. remove false certainty from any alliance packet
4. prepare the final #BLINKSYNC wrap-up shell for the completed rebuild packet

## Copilot — Required Outputs
1. scaffold repo hygiene:
   - README
   - docs/
   - manifests/
   - scripts/
   - handoffs/
2. convert approved planning outputs into clean Markdown and implementation checklists
3. prepare issue templates / PR structure only after mapping is approved
4. do not invent infrastructure; implement only approved structure
5. keep workflows OIDC-ready, but do not force deployment logic before mapping is complete

## Current Objective
Build the planning-and-execution repo that will hold:
- GitHub scanning manifest
- scan results
- repo-to-cloud mapping report
- Mythosblock rebuild manifest
- corrected new-codex scripts
- final command sequence
- final alliance handoff

## Immediate Next Outputs
1. repo scanning command set
2. repo-to-cloud mapping report
3. Mythosblock rebuild manifest
4. corrected new-codex scripts
5. final command sequence
6. final #BLINKSYNC handoff

End of directive.
